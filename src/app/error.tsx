// "use client" Error should be client side. to make a page client side just add use client as string at the top of the page 
"use client"
export default function Error(){
    return(
    <div>
    My error Page
    </div>
    )}